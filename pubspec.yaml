name: sipantaw_mobile
description: SIPANTAW - Sistem Monitoring WFA

publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  dio: ^5.7.0
  shared_preferences: ^2.3.0
  flutter_secure_storage: ^9.2.0
  go_router: ^14.0.0
  provider: ^6.1.0
  image_picker: ^1.1.0
  geolocator: ^13.0.0
  permission_handler: ^11.0.0
  intl: ^0.19.0
  cached_network_image: ^3.4.0
  file_picker: ^8.0.0+1
  fl_chart: ^0.68.0
  local_auth: ^2.3.0
  camera: ^0.11.0
  google_mlkit_face_detection: ^0.11.0
  flutter_animate: ^4.5.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true